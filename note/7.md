# Phenix

# Install

`phx.new` パッケージをインストールして、phenix のプロジェクトを初期化する。

```sh
mix archive.install hex phx_new 1.5.8
mix phx.new {project_name}
```

# Server

```sh
mix phx.server
```

localhost:4000 で立ち上がる